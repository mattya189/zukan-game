# このリポジトリで作業するときのルール

スマホ向けのブラウザゲーム「ガチャ図鑑」。GitHub Pages で公開し、サーバーは使わない（記録は各端末の localStorage）。
作者はスマホだけで開発しているため、**手元でビルドや npm install はできない**前提で作業すること。

## 守ること

1. **ビルド不要**：フレームワークやバンドラーを導入しない。素の HTML / CSS / JavaScript のまま保つ。外部CDNも増やさない（現在の依存はGoogle Fontsのみ）。
2. **日本語**：コメント、UIの文言、プルリクエストの説明はすべて日本語。専門用語は避け、やさしい言葉で書く。
3. **スマホ縦画面が基準**：幅360〜430px、高さ600〜930pxで崩れないこと。バトル画面はスクロールなしで収まること。
4. **キャラの id は不変**：`chr_031` のような id は、保存データとつながっている。変更・削除・番号の詰め直しをしない。キャラをやめるときは非表示フラグを足す形で提案する。
5. **保存データを壊さない**：`localStorage` のキーは `gachaZukanDemo_v2`。構造を変えるときは、古い保存データを読み込める移行処理（`_migrate`）を必ず書く。
6. **ゲームのルールは GameServer に集約**：将来サーバーへ移すため、抽選・報酬・時間・バトルの判定は `GameServer` と `Battle` の中だけに書く。画面側（UI）に判定を書かない。
7. **1回の作業で1つのこと**：頼まれた変更だけを行う。ついでの整形や書き換えをしない。
8. **数値を変えたら検証する**：`node tools/sim.js` を実行し、変更前後の勝率・出現率を説明に書く。
9. **画像の差し替えは版番号を上げる**：`chr_022_base_1.webp` → `chr_022_base_2.webp` とし、`data/characters.js` の `art` の数字を上げる。同じ名前で上書きしない。

## ファイルの役割

| ファイル | 中身 | 変更の頻度 |
|---|---|---|
| `index.html` | 画面の枠とCSS、データの読み込み | まれ |
| `js/game.js` | ゲーム本体（GameServer / Battle / 画面） | 機能追加のとき |
| `data/characters.js` | キャラ一覧と縁（BONDS） | キャラ追加のたび |
| `data/artifacts.js` | アーティファクト一覧 | 追加のたび |
| `data/quests.js` | クエスト一覧 | 追加のたび |
| `data/rules.js` | 数値・ルール・称号・バトルの部品 | 調整のたび |
| `stories/chr_xxx.js` | 図鑑の文章（開いたときに読み込む） | キャラ追加のたび |
| `img/` | キャラ画像（大512px / 小160px の webp） | キャラ追加のたび |
| `tools/sim.js` | バランス確認スクリプト（Node.js） | まれ |
| `docs/` | 仕様・やること一覧・依頼の書き方 | 随時 |

## 確認のしかた

```bash
node --check js/game.js        # 文法エラーがないか
node tools/sim.js              # 対戦・クエスト・個体値の統計
node tools/sim.js battles 5000 # 対戦だけ多めに
```

画面の確認は作者がスマホで行う。**変更点と、どの画面のどこを見ればよいかを説明に書くこと。**

## プルリクエストに書くこと

- 何を変えたか（箇条書き）
- 触ったファイル
- `node tools/sim.js` の結果（数値を変えた場合は変更前後）
- 作者が確認すべき画面と手順
- 保存データへの影響（ある場合）
